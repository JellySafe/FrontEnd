import { CurrentLocationScreen } from "@/features/location/components/CurrentLocationScreen";

// 현재 위치 찾기 화면 조립. 실제 로직은 client 컴포넌트에 위임.
export default function CurrentLocationPage() {
  return <CurrentLocationScreen />;
}
