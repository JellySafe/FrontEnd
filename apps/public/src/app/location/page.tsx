import { LocationSetupScreen } from "@/features/location/components/LocationSetupScreen";

// 위치 설정 화면 조립. 실제 로직은 client 컴포넌트에 위임.
export default function LocationPage() {
  return <LocationSetupScreen />;
}
